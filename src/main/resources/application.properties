# GENERAL
server.port=8080

# DATABASE
spring.datasource.url=jdbc:mysql://localhost/db_curso_spring?useSSL=false
spring.datasource.dbname=db_curso_spring
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JWT
security.jwt.secret=miskamuscamikimouse
# service name
security.jwt.issuer=Main
# sesion expiration time (hours * 3600000) the following example expire in a week
security.jwt.ttlMillis=604800000

